function Loader() {
  return (
    <div
      style={{
        color: "var(--slate-300)",
        display: "flex",
        alignItems: "center",
        gap: "1rem",
      }}
    >
      <p>Loading Data ...</p>
    </div>
  );
}

export default Loader;
